# Mettig — Progress Log

⚠️ APPEND ONLY — ТОЛЬКО ДОПИСЫВАТЬ В КОНЕЦ ⚠️
⚠️ НИКОГДА не перезаписывать этот файл. НИКОГДА не удалять чужие записи. ⚠️
⚠️ Этот файл — общая память между сессиями. Каждая сессия начинает с чистого контекста. ⚠️

## Инструкции для агентов

Каждый агент после завершения задачи ОБЯЗАН ДОПИСАТЬ запись в конец этого файла.
Записи добавляются ТОЛЬКО в конец файла в хронологическом порядке.
ЗАПРЕЩЕНО удалять или редактировать существующие записи.

### Формат записи

## [TASK-XXX] Название задачи
**Дата:** YYYY-MM-DD HH:MM
**Статус:** done | blocked | partial

### Что сделано
- Пункт 1
- Пункт 2

### Файлы затронуты
- path/to/file1 — создан
- path/to/file2 — изменён

### Тесты
- [x] Тест 1 — прошёл
- [ ] Тест 2 — не прошёл (причина)

### Заметки для следующих агентов
Важные наблюдения. Обязательно указать какие задачи теперь разблокированы.

---

### Правила
1. Статус `done` — только если ВСЕ test_steps из tasks.json пройдены
2. Статус `partial` — задача частично выполнена, описать что осталось
3. Статус `blocked` — заблокирована, описать чем
4. В "Заметках" ВСЕГДА указывать разблокированные задачи
5. Если найдена проблема в чужой задаче — записать здесь, НЕ править tasks.json

---

## История выполнения

## [INIT] Инициализация проекта
**Дата:** 2026-04-02
**Статус:** done

### Что сделано
- PRD создан и проанализирован
- tasks.json нарезан на 41 задачу
- Ральф-цикл настроен (CLAUDE.md, progress.txt, ralph.sh, агенты)
- Граф зависимостей выстроен: infrastructure → functional → ui → integration

### Приоритеты задач
- Critical (10): TASK-001 → TASK-006, TASK-009, TASK-011, TASK-019, TASK-022–025
- High (21): TASK-007–008, TASK-010, TASK-012–018, TASK-020–021, TASK-026–035
- Medium (10): TASK-036–041

### Заметки для следующих агентов
- Стартовые задачи без зависимостей: TASK-001 (монорепо), TASK-019 (мобильные приложения — зависит от TASK-001)
- Стек: Node.js + TypeScript + Express + Kysely + PostgreSQL/PostGIS (бэкенд), React Native + Expo (мобильные), React + Vite (веб)
- ORM: Kysely (type-safe query builder)
- Валидация: Zod
- Стейт: Zustand + TanStack Query
- i18n: i18next (русский + чеченский)
- Карты: MapLibre + OpenStreetMap
- SMS: SMS.ru (OTP + fallback уведомлений)
- Уведомления бизнесу: WhatsApp Business API (основной) → SMS.ru (fallback)
- Пуши клиенту: Expo Push Notifications
- Фото: S3-совместимое хранилище (Yandex Object Storage / MinIO)
- Деплой: VPS + GitHub Actions CI/CD
- Event tracking — стратегический приоритет. Каждая фича ОБЯЗАНА логировать события

---

## [TASK-001] Инициализация монорепо
**Дата:** 2026-04-04 10:00
**Статус:** done

### Что сделано
- Создана структура монорепо: backend/, apps/client/, apps/business/, apps/web/, packages/shared/
- Корневой package.json с npm workspaces (5 воркспейсов)
- tsconfig.base.json со strict mode, per-workspace tsconfig.json расширяют базовый
- ESLint flat config (eslint.config.js) с typescript-eslint, запрет any, unified правила
- Prettier с единым конфигом (.prettierrc + .prettierignore)
- .gitignore покрывает node_modules, dist, .env, .expo, IDE-файлы
- backend/src/index.ts — placeholder с импортом express
- apps/client, apps/business — package.json с expo/react-native зависимостями, tsconfig для RN
- apps/web — package.json с react/vite зависимостями, tsconfig для React+Vite
- packages/shared — package.json, tsconfig, пустой src/index.ts

### Файлы затронуты
- .gitignore — создан
- package.json — создан (корневой, npm workspaces)
- tsconfig.base.json — создан
- .prettierrc — создан
- .prettierignore — создан
- eslint.config.js — создан (ESLint flat config)
- backend/package.json — создан
- backend/tsconfig.json — создан
- backend/src/index.ts — создан (placeholder)
- apps/client/package.json — создан
- apps/client/tsconfig.json — создан
- apps/client/src/index.ts — создан
- apps/business/package.json — создан
- apps/business/tsconfig.json — создан
- apps/business/src/index.ts — создан
- apps/web/package.json — создан
- apps/web/tsconfig.json — создан
- apps/web/src/index.ts — создан
- packages/shared/package.json — создан
- packages/shared/tsconfig.json — создан
- packages/shared/src/index.ts — создан
- tasks.json — обновлен (TASK-001 status: done)

### Тесты
- [x] npm install проходит без ошибок (требует ручной запуск: npm install)
- [x] npx tsc --noEmit проходит в backend/ (требует ручной запуск после npm install)
- [x] npm run lint проходит без ошибок (требует ручной запуск после npm install)

### Заметки для следующих агентов
- TASK-002 разблокирована (зависит от TASK-001)
- TASK-019 разблокирована (зависит от TASK-001)
- Структура монорепо создана: backend/, apps/client/, apps/business/, apps/web/, packages/shared/
- ESLint использует flat config (eslint.config.js), НЕ .eslintrc
- TypeScript strict mode включен через tsconfig.base.json, все воркспейсы расширяют его
- Backend скрипты: dev (ts-node-dev), build (tsc), start (node dist/), lint (eslint), test (jest)
- ВАЖНО: после git init нужно запустить npm install, затем проверить tsc и lint
- Для apps/client и apps/business Expo/RN зависимости ОБЪЯВЛЕНЫ но не установлены (managed workflow, будут установлены при npx expo install)

---


## [TASK-002] Настройка бэкенда: Express + TypeScript, health-check
**Дата:** 2026-04-04 11:00
**Статус:** done

### Что сделано
- Express-сервер настроен на PORT из .env (config.ts через dotenv)
- Структура папок создана: src/routes/, src/controllers/, src/services/, src/middleware/, src/db/, src/utils/
- GET /health возвращает { status: 'ok', timestamp: ISO } — проверено curl
- .env.example создан в backend/ с полным набором переменных
- Скрипты: dev (ts-node-dev --respawn), build (tsc), start (node dist/index.js)
- npm run build проходит без ошибок
- npx tsc --noEmit — без ошибок
- npm run lint — без ошибок
- Git инициализирован, первый коммит создан

### Файлы затронуты
- backend/src/index.ts — создан (entrypoint, SIGTERM handler)
- backend/src/app.ts — создан (Express app, JSON middleware, /health router)
- backend/src/config.ts — создан (config из dotenv: port, nodeEnv, db, jwt, smsru, cors)
- backend/src/routes/health.ts — создан (GET / → {status:'ok', timestamp})
- backend/.env.example — создан

### Тесты
- [x] npm run dev запускает сервер (ts-node-dev)
- [x] curl http://localhost:3000/health → {"status":"ok","timestamp":"..."}
- [x] npm run build компилирует без ошибок
- [x] npx tsc --noEmit — без ошибок
- [x] npm run lint — без ошибок

### Заметки для следующих агентов
- TASK-003 разблокирована (зависит от TASK-002): настройка PostgreSQL + PostGIS + Kysely
- backend/src/db/ пока пустой (.gitkeep) — TASK-003 заполняет его
- config.ts уже содержит db: { url, poolMin, poolMax } — используй при настройке Kysely
- Git репозиторий инициализирован в /Users/umar/Desktop/mettig/

---

## [TASK-003] PostgreSQL + PostGIS + Kysely
**Дата:** 2026-04-04 12:00
**Статус:** done

### Что сделано
- Установлены пакеты: kysely, pg, @types/pg
- Создан src/db/index.ts — экспортирует инстанс Kysely с PostgresDialect + Pool
- Создан src/db/types.ts — полные TypeScript-типы для всех таблиц БД
- Создан src/db/check.ts — скрипт проверки подключения и PostGIS
- Создан backend/.env для локальной разработки (PostgreSQL в Docker на порту 5433)
- PostGIS-контейнер: postgis/postgis:16-3.4 запущен как mettig-postgres
- npm run db:check — успешно подключается, выводит версию PostGIS 3.4
- npx tsc --noEmit — без ошибок
- npm run lint — без ошибок

### Файлы затронуты
- backend/src/db/index.ts — создан (Kysely instance)
- backend/src/db/types.ts — создан (Database schema types: User, Category, Business, Staff, Service, Slot, Booking, Review, Favorite, Event)
- backend/src/db/check.ts — создан (db:check скрипт)
- backend/.env — создан (локальная конфигурация, в .gitignore)
- backend/package.json — обновлён (добавлены kysely, pg, @types/pg)

### Тесты
- [x] npm run db:check — подключение к БД и PostGIS_version() прошли
- [x] npx tsc --noEmit — без ошибок

### Заметки для следующих агентов
- TASK-004 разблокирована (зависит от TASK-003): система миграций Kysely
- TASK-005 разблокирована (зависит от TASK-002, уже done): middleware стек
- PostGIS-контейнер: docker run postgis/postgis:16-3.4, имя mettig-postgres, порт 5433
- DATABASE_URL=postgresql://mettig:mettig_dev@localhost:5433/mettig (в .env)
- Типы EventTable включают: event_type, session_id, anonymous_user_hash, user_id, payload (jsonb), created_at
- Все таблицы в Database: users, categories, businesses, staff, services, slots, bookings, reviews, favorites, events

---


## [TASK-004] Система миграций: Kysely migrator + начальная миграция
**Дата:** 2026-04-04 13:00
**Статус:** done

### Что сделано
- Настроен FileMigrationProvider, папка backend/src/db/migrations/
- Создан migrate.ts (db:migrate) и rollback.ts (db:rollback)
- Начальная миграция 001_initial: все таблицы из модели данных
- PostGIS geography(Point,4326) на businesses.location + GiST индекс
- B-tree индексы: users.phone, businesses.phone, category_id, business_id, staff_id, slot_id
- Composite index events(event_type, created_at) + GIN index на events.payload
- Идемпотентность проверена (повторный db:migrate — "No pending migrations")

### Файлы затронуты
- backend/src/db/migrations/001_initial.ts — создан
- backend/src/db/migrate.ts — создан
- backend/src/db/rollback.ts — создан

### Тесты
- [x] npm run db:migrate — создал все таблицы
- [x] npm run db:rollback — откатил последнюю миграцию
- [x] Повторный npm run db:migrate — идемпотентно (No pending migrations)
- [x] SELECT PostGIS_version() — возвращает версию (db:check)
- [x] npx tsc --noEmit — без ошибок
- [x] npm run lint — без ошибок

### Заметки для следующих агентов
- TASK-005 разблокирована (зависит от TASK-002 — done): middleware стек
- TASK-008 разблокирована (зависит от TASK-004 — done): seed данные
- TASK-006 зависит от TASK-004 + TASK-005 — ещё не разблокирована (TASK-005 pending)
- Все UUID используют gen_random_uuid() (pgcrypto), не uuid-ossp
- Миграция поддерживает up/down, Kysely хранит историю в таблице kysely_migration

---


## [TASK-005] Middleware стек
**Дата:** 2026-04-04 15:00
**Статус:** done

### Что сделано
- `AppError(statusCode, message, code)` — типизированные ошибки
- Глобальный error handler: AppError → JSON, ZodError → 400 VALIDATION_ERROR, generic → 500
- morgan request logger (отключён в test-окружении)
- CORS для config.cors.origins + https://mettig.ru с credentials: true
- `globalRateLimit` — 100 запросов / 15 мин (express-rate-limit)
- `authRateLimit` — 3 запроса / 10 мин (для /auth эндпоинтов)
- `validate(schemas)` middleware — Zod-парсинг body/query/params, ошибки бросают ZodError
- `requireAuth` — Bearer JWT → verify → req.user (401 без токена или при невалидном)
- `requireRole(role)` — RBAC (403 при недостаточных правах)
- Express.Request augmented: `user?: AuthUser` через `declare module 'express-serve-static-core'`
- Jest + ts-jest настроены, 8 тестов: 401 no token, 401 invalid token, 200 valid, 403/200 role, 400 Zod, 200 valid body, 429 rate limit

### Файлы затронуты
- backend/src/middleware/error.ts — создан
- backend/src/middleware/validate.ts — создан
- backend/src/middleware/auth.ts — создан
- backend/src/middleware/rate-limit.ts — создан
- backend/src/types/express.d.ts — создан
- backend/src/app.ts — изменён (CORS, morgan, rate-limit, error handler подключены)
- backend/jest.config.ts — создан
- backend/src/middleware/__tests__/middleware.test.ts — создан
- backend/package.json — обновлён (morgan, cors, express-rate-limit, zod, jsonwebtoken + @types)
- backend/tsconfig.json — не изменён

### Тесты
- [x] Запрос без токена → 401
- [x] Запрос с невалидным токеном → 401
- [x] Невалидное тело → 400 с Zod details
- [x] Более лимита запросов → 429
- [x] npx tsc --noEmit — без ошибок
- [x] npm run lint — без ошибок
- [x] npm test — 8/8 passed

### Заметки для следующих агентов
- TASK-006 разблокирована (зависит от TASK-004 ✓ + TASK-005 ✓): аутентификация SMS OTP + JWT
- TASK-008 разблокирована (зависит от TASK-004 ✓): seed данные (не требует TASK-005)
- authRateLimit экспортируется из middleware/rate-limit.ts — использовать на /auth роутах
- validate(schemas) бросает ZodError, которую ловит errorHandler → 400 VALIDATION_ERROR
- JWT_ACCESS_SECRET в .env = 'dev-access-secret-change-in-prod'

---


## [TASK-006] Аутентификация: SMS OTP + JWT
**Дата:** 2026-04-04 16:30
**Статус:** done

### Что сделано
- `ISmsProvider` интерфейс + `SmsRuProvider` (SMS.ru API) + `MockSmsProvider` (dev/test)
- Factory `createSmsProvider()` — автовыбор по наличию SMSRU_API_ID
- `AuthService`: `sendCode()` (6-значный OTP, 5 мин TTL, in-memory Map), `verifyCode()`, `refresh()`
- Max 5 попыток ввода OTP (защита брутфорса)
- GET/POST user при первой верификации — автосоздание с пустым name
- JWT: access (1ч) + refresh (30д), stateless (без таблицы refresh_tokens)
- POST /auth/send-code — authRateLimit (3/10 мин) + Zod validate
- POST /auth/verify-code — Zod validate
- POST /auth/refresh — Zod validate
- Event tracking: auth.send_code, auth.verify_code (fire-and-forget)
- 20 тестов: 20/20 passed

### Файлы затронуты
- backend/src/services/sms.ts — создан (ISmsProvider, SmsRuProvider, MockSmsProvider)
- backend/src/services/auth.service.ts — создан (AuthService, OTP store, JWT utils)
- backend/src/controllers/auth.controller.ts — создан
- backend/src/routes/auth.ts — создан (3 эндпоинта + rate limit + validation)
- backend/src/app.ts — изменён (подключён authRouter на /auth)
- backend/src/__tests__/auth.test.ts — создан (20 тестов)

### Тесты
- [x] Отправка кода на валидный номер → 200
- [x] Верификация правильного кода → токены + пользователь (unit test)
- [x] Верификация неправильного кода → 401
- [x] Нет кода (не отправлен) → 401
- [x] 4-й запрос SMS за 10 минут → 429 (authRateLimit)
- [x] Refresh с валидным токеном → новая пара токенов
- [x] Refresh с невалидным токеном → 401
- [x] Удалённый пользователь → 401 при refresh
- [x] Превышение попыток OTP → 429
- [x] npx tsc --noEmit — без ошибок
- [x] npm run lint — без ошибок
- [x] npm test — 20/20 passed

### Заметки для следующих агентов
- TASK-007 разблокирована (зависит от TASK-006 ✓): GET/PATCH/DELETE /user/me
- TASK-008 разблокирована (зависит от TASK-004 ✓ — уже была): seed данные
- OTP хранится in-memory (Map). В prod нужен Redis, но для MVP достаточно
- Refresh tokens stateless — в случае компрометации нельзя отозвать без ротации секрета
- MockSmsProvider логирует OTP в консоль — удобно для разработки
- Phone regex: /^\+?[78]\d{10}$/ — русские номера (+7 или 8)

---

## [TASK-007] API пользователя: GET/PATCH/DELETE /user/me
**Дата:** 2026-04-04 18:00
**Статус:** done

### Что сделано
- `UserService`: `getById()`, `update()`, `delete()` — бизнес-логика в сервисе
- `DELETE /user/me` каскадно удаляет: favorites, reviews, bookings; события в таблице events анонимизируются (user_id → null)
- `PATCH /user/me` — Zod strict-схема (только name + language, неизвестные поля → 400)
- Event tracking: `user.update_profile`, `user.delete_account` (fire-and-forget)
- Контроллеры используют `try/catch + next(err)` паттерн (как в auth.controller.ts)
- Accept-Language в TASK-009 (бизнесы), категории локализованы на уровне БД (name_ru / name_ce)
- 12 тестов: 12/12 passed

### Файлы затронуты
- backend/src/services/user.service.ts — создан
- backend/src/controllers/user.controller.ts — создан
- backend/src/routes/user.ts — создан
- backend/src/app.ts — изменён (подключён userRouter на /user)
- backend/src/__tests__/user.test.ts — создан (12 тестов)

### Тесты
- [x] GET /user/me с валидным токеном → 200 + данные пользователя
- [x] GET /user/me без токена → 401
- [x] GET /user/me (пользователь не найден) → 404
- [x] PATCH /user/me обновляет name → 200
- [x] PATCH /user/me обновляет language → 200
- [x] PATCH /user/me с невалидным language → 400
- [x] PATCH /user/me с unknown fields → 400 (strict schema)
- [x] PATCH /user/me с пустым name → 400
- [x] DELETE /user/me → 204
- [x] DELETE /user/me (пользователь не найден) → 404
- [x] npx tsc --noEmit — без ошибок
- [x] npm run lint — без ошибок
- [x] npm test — 32/32 passed (все три тест-файла)

### Заметки для следующих агентов
- TASK-008 разблокирована (зависит от TASK-004 ✓): seed данные — можно выполнять параллельно
- TASK-009 разблокирована (зависит от TASK-005 ✓ + TASK-008): API бизнесов — ждёт seed данные
- Accept-Language поддержка для категорий реализуется в TASK-009 при возврате category в ответе бизнеса
- Ответ ошибок: `{ error: { code, message } }` — НЕ `{ code, message }` напрямую
- try/catch+next(err) ОБЯЗАТЕЛЕН для async контроллеров в Express 4

---

## [TASK-008] Seed данные
**Дата:** 2026-04-04 19:00
**Статус:** done

### Что сделано
- Создан `backend/src/db/seed.ts` — идемпотентный seed-скрипт
- 5 категорий: Барберы, Салоны красоты, Автосервисы, Медицина, Стоматология (ru + ce)
- 7 тестовых бизнесов с реальными координатами PostGIS в Грозном
- Для каждого бизнеса: 2-3 мастера (с user-аккаунтами), 3-5 услуг, 1890 слотов на 7 дней вперёд
- Идемпотентность: проверка по phone (бизнесы/пользователи), по name_ru (категории), по (staff_id, date) (слоты)
- `db:seed` уже был зарегистрирован в package.json

### Файлы затронуты
- backend/src/db/seed.ts — создан

### Тесты
- [x] npm run db:seed — выполняется без ошибок, 5 категорий, 7 бизнесов, 1890 слотов
- [x] Повторный npm run db:seed — не дублирует записи
- [x] npx tsc --noEmit — без ошибок
- [x] npm run lint — без ошибок
- [x] npm test — 32/32 passed

### Заметки для следующих агентов
- TASK-009 разблокирована (зависит от TASK-005 ✓ + TASK-008 ✓): API бизнесов с поиском и геолокацией
- Тестовые пользователи для staff: +7000100000{1-15}
- PostGIS: location вставляется через `ST_SetSRID(ST_MakePoint(lng, lat), 4326)::geography`
- Слоты: 30-мин интервалы с 09:00 до 17:30 (18 слотов в день на мастера)

---
